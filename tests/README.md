# The tests Directory

This directory contains pytests that are used to test the functionality of Maeser.

- To run the pytests, run `make test` from the project root.
- To run the pytests with verbose printing, run `make testVerbose` from the project root.
