# The sphinx-docs Directory

This directory contains the documentation for Maeser.

- **To install documentation dependencies**, run `make deps`.
- **To build the documentation**, run `make html`.

The resultant build files are stored in `_build/html/`. Open `_build/html/index.html` in your browser to access the documentation home page.
